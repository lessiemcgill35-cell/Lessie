# Blue Blur Adventure

Run locally by opening `application/index.html` in a browser.

For a simple local server (recommended for some browsers):

```bash
cd application
python3 -m http.server 8080
```

Then open `http://localhost:8080`.
